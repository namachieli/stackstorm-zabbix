---
name: delete.hostgroup
pack: zabbix
runner_type: python-script
description: "Delete host groups by ID."
enabled: true
entry_point: call_api.py
parameters:
  api_method:
    default: "hostgroup.delete"
    immutable: true
  params_list:
    type: array
    description: "List of host group IDs to delete."
    required: true
