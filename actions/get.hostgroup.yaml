---
name: get.hostgroup
pack: zabbix
runner_type: python-script
description: "Get a host group by ID."
enabled: true
entry_point: call_api.py
parameters:
  api_method:
    default: "hostgroup.get"
    immutable: true
  groupids:
    type: array
    description: "Host group IDs to retrieve."
    required: true
  output:
    type: string
    description: "Output fields. Use 'extend' for all."
    default: "extend"
