---
name: get.template
pack: zabbix
runner_type: python-script
description: "Get a template by ID."
enabled: true
entry_point: call_api.py
parameters:
  api_method:
    default: "template.get"
    immutable: true
  templateids:
    type: array
    description: "Template IDs to retrieve."
    required: true
  output:
    type: string
    description: "Output fields. Use 'extend' for all."
    default: "extend"
