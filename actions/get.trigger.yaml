---
name: get.trigger
pack: zabbix
runner_type: python-script
description: "Get a trigger by ID."
enabled: true
entry_point: call_api.py
parameters:
  api_method:
    default: "trigger.get"
    immutable: true
  triggerids:
    type: array
    description: "Trigger IDs to retrieve."
    required: true
  output:
    type: string
    description: "Output fields. Use 'extend' for all."
    default: "extend"
  expandDescription:
    type: boolean
    description: "Expand macros in trigger description."
    required: false
