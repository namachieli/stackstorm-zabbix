---
name: create.correlation
pack: zabbix
runner_type: python-script
description: "Create an event correlation rule."
enabled: true
entry_point: call_api.py
parameters:
  api_method:
    default: "correlation.create"
    immutable: true
  name:
    type: string
    description: "Name of the correlation."
    required: true
  filter:
    type: object
    description: "Correlation filter."
    required: true
  operations:
    type: array
    description: "Correlation operations."
    required: true
  status:
    type: integer
    description: "Status: 0 (enabled), 1 (disabled)."
    required: false
  description:
    type: string
    description: "Description."
    required: false
