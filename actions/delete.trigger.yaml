---
name: delete.trigger
pack: zabbix
runner_type: python-script
description: "Delete triggers by ID."
enabled: true
entry_point: call_api.py
parameters:
  api_method:
    default: "trigger.delete"
    immutable: true
  params_list:
    type: array
    description: "List of trigger IDs to delete."
    required: true
