---
name: delete.proxy
pack: zabbix
runner_type: python-script
description: "Delete proxies by ID."
enabled: true
entry_point: call_api.py
parameters:
  api_method:
    default: "proxy.delete"
    immutable: true
  params_list:
    type: array
    description: "List of proxy IDs to delete."
    required: true
