---
name: delete.correlation
pack: zabbix
runner_type: python-script
description: "Delete event correlations by ID."
enabled: true
entry_point: call_api.py
parameters:
  api_method:
    default: "correlation.delete"
    immutable: true
  params_list:
    type: array
    description: "List of correlation IDs to delete."
    required: true
